import 'package:flutter/material.dart';

class BuildLoadingTodos extends StatelessWidget {
  const BuildLoadingTodos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
